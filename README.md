# Custom-IPsec-S2S
Here I have configured IPsec Site-To-Site VPN on the fortigate firewall. Using the custom mode for more granularity in choosing the algorithms and dh group. The site-to-site tunnel is a permanent tunnel compared to the dial-up which is temporary. Things that we need to configure is >IPsec tunnel configuration >Static Routes >Policies. 
